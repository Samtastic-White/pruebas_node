services:
  frontend_seed:
    container_name: frontend_seed
    restart: unless-stopped
    build:
      context: .
      dockerfile: Dockerfile.local
    ports:
      - "3000:5173"
    networks:
      - marathon_network

networks:
  marathon_network:
    driver: bridge