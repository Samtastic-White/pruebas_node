export enum EventStatus {
  ACTIVE = 'active',
  INACTIVE = 'inactive',
  FINISHED = 'finished',
}

export enum RegistrationStatus {
  CONFIRMED = 'confirmed',
  CANCELLED = 'cancelled',
}

export enum AdminRole {
  ADMIN = 'admin',
  //SUPERADMIN = 'superadmin',
}